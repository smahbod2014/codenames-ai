# The base URL for the Codenames backend API
# For local development, this will typically be http://localhost:8080
# For production, this should be the URL of your deployed backend service (e.g., from Render)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
